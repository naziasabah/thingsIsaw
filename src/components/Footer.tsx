export default function Footer() {
  return (
    <footer className="pointer-events-none absolute inset-x-0 bottom-0 z-50 flex justify-center px-6 py-6 sm:px-10 sm:py-8">
      <span className="font-mono text-[10px] uppercase tracking-[0.22em] text-black/40 sm:text-[11px]">
        A visual archive, ongoing
      </span>
    </footer>
  );
}
