export default function Header() {
  return (
    <header className="pointer-events-none absolute inset-x-0 top-0 z-50 flex items-start justify-center px-6 py-6 sm:px-10 sm:py-8">
      <span className="pointer-events-auto font-mono text-[11px] font-semibold uppercase tracking-[0.22em] text-black sm:text-xs">
        Things I Saw
      </span>
    </header>
  );
}
