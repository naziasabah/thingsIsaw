# Photos

Drop image files (`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.avif`) directly into this
folder and push. The gallery reads whatever is here at request time and displays it —
no upload UI, no backend, no storage service.

This file itself is ignored by the gallery (only image files are picked up).
